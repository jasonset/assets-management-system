package com.gdn.warehouse.assetsmanagement.enums;

public enum MaintenanceStatus {
   REQUEST_PENDING,
   REQUEST_DECLINED,
   REQUEST_RECEIVED,
   SCHEDULED,
   ON_MAINTENANCE,
   DONE,
   REJECTED
}
